import { render, screen } from '@testing-library/react';
import App from '../App';

describe('test navbar', () => {
    describe('when user clicked on logo, redirect to home page', () => {});

    describe('when click on link, check if the redirection is valid', () => {
        // ex. click on link project redirect to route /projects
    });
});
