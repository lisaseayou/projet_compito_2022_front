import { render, screen } from '@testing-library/react';
import App from '../App';

describe('add project', () => {
    describe('', () => {
        it('project is added', () => {});
    });

    describe('returns no error', () => {});
    
});
