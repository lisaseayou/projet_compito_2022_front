import { render, screen } from '@testing-library/react';
import App from '../App';
import { MockedProvider } from '@apollo/client/testing';

describe('404 showing', () => {
    it('render error', () => {});

    describe('If route not exist, redirect to route NotFound', () => {});

    describe('check if link to login when user is not connected', () => {});
});
