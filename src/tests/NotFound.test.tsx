import { render, screen } from "@testing-library/react";
import App from "../App";
import {MockedProvider} from "@apollo/client/testing";

describe('404 showing', () => {
    it('render error', () => {

    })
})