import { render, screen } from '@testing-library/react';
import App from '../App';

describe('profile informations', () => {
    it('user fullname', () => {});

    describe('display user informations are ok', () => {
        it('display description', () => {});
        it('add site web and social media', () => {});
        it('update user infos', () => {});
    });
});
