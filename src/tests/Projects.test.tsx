import { render, screen } from '@testing-library/react';
import App from '../App';

describe('all projects', () => {
    it('projects have been fetch', () => {});

    describe('filters project', () => {
        it('by created at', () => {});

        it('by name', () => {});
    });
});
