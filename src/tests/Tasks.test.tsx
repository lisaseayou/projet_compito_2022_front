import { render, screen } from '@testing-library/react';
import App from '../App';

describe('all tasks', () => {
    describe('tasks have been fetch', () => {});
    describe('tasks with no result', () => {});

    describe('filter by', () => {
        it('created at', () => {});
        it('name', () => {});
        it('status', () => {});
        it('advancement', () => {});
        it('updated at', () => {});
    });
});